# Go 并发模式 - 完整教程

> @author erik.zhou  
> 更新时间：2025-03-06

## 目录
- [并发模式概述](#并发模式概述)
- [基础模式](#基础模式)
- [高级模式](#高级模式)
- [实战案例](#实战案例)
- [最佳实践](#最佳实践)

## 并发模式概述

Go 语言的并发模式基于 CSP（Communicating Sequential Processes）理论，通过 goroutine 和 channel 实现。

### 核心理念

> Don't communicate by sharing memory; share memory by communicating.
> 不要通过共享内存来通信，而应该通过通信来共享内存。

## 基础模式

### 1. Generator 模式

生成器模式用于生成一系列值。

```go
package main

import "fmt"

// 生成器函数
func generator(nums ...int) <-chan int {
    out := make(chan int)
    go func() {
        defer close(out)
        for _, n := range nums {
            out <- n
        }
    }()
    return out
}

func main() {
    // 使用生成器
    for num := range generator(1, 2, 3, 4, 5) {
        fmt.Println(num)
    }
}
```

### 2. Pipeline 模式

管道模式将数据处理分解为多个阶段。

```go
package main

import "fmt"

// 阶段 1：生成数字
func generate(nums ...int) <-chan int {
    out := make(chan int)
    go func() {
        defer close(out)
        for _, n := range nums {
            out <- n
        }
    }()
    return out
}

// 阶段 2：平方
func square(in <-chan int) <-chan int {
    out := make(chan int)
    go func() {
        defer close(out)
        for n := range in {
            out <- n * n
        }
    }()
    return out
}

// 阶段 3：过滤偶数
func filterEven(in <-chan int) <-chan int {
    out := make(chan int)
    go func() {
        defer close(out)
        for n := range in {
            if n%2 == 0 {
                out <- n
            }
        }
    }()
    return out
}

func main() {
    // 构建管道
    nums := generate(1, 2, 3, 4, 5)
    squared := square(nums)
    filtered := filterEven(squared)
    
    // 消费结果
    for n := range filtered {
        fmt.Println(n)  // 输出：4, 16
    }
}
```

### 3. Fan-Out/Fan-In 模式

Fan-Out：将任务分发给多个 worker  
Fan-In：将多个 worker 的结果合并

```go
package main

import (
    "fmt"
    "sync"
)

// Fan-Out：启动多个 worker
func fanOut(in <-chan int, workers int) []<-chan int {
    channels := make([]<-chan int, workers)
    for i := 0; i < workers; i++ {
        channels[i] = worker(in)
    }
    return channels
}

// Worker 函数
func worker(in <-chan int) <-chan int {
    out := make(chan int)
    go func() {
        defer close(out)
        for n := range in {
            out <- n * n  // 处理数据
        }
    }()
    return out
}

// Fan-In：合并多个 channel
func fanIn(channels ...<-chan int) <-chan int {
    out := make(chan int)
    var wg sync.WaitGroup
    
    for _, ch := range channels {
        wg.Add(1)
        go func(c <-chan int) {
            defer wg.Done()
            for n := range c {
                out <- n
            }
        }(ch)
    }
    
    go func() {
        wg.Wait()
        close(out)
    }()
    
    return out
}

func main() {
    // 生成数据
    in := make(chan int)
    go func() {
        defer close(in)
        for i := 1; i <= 10; i++ {
            in <- i
        }
    }()
    
    // Fan-Out 到 3 个 worker
    workers := fanOut(in, 3)
    
    // Fan-In 合并结果
    result := fanIn(workers...)
    
    // 输出结果
    for n := range result {
        fmt.Println(n)
    }
}
```

### 4. Worker Pool 模式

工作池模式用于限制并发数量。

```go
package main

import (
    "fmt"
    "sync"
    "time"
)

type Job struct {
    ID int
}

type Result struct {
    Job Job
    Sum int
}

// Worker 函数
func worker(id int, jobs <-chan Job, results chan<- Result, wg *sync.WaitGroup) {
    defer wg.Done()
    for job := range jobs {
        fmt.Printf("Worker %d processing job %d\n", id, job.ID)
        time.Sleep(time.Second)  // 模拟工作
        
        results <- Result{
            Job: job,
            Sum: job.ID * 2,
        }
    }
}

func main() {
    const numJobs = 10
    const numWorkers = 3
    
    jobs := make(chan Job, numJobs)
    results := make(chan Result, numJobs)
    
    // 启动 worker pool
    var wg sync.WaitGroup
    for i := 1; i <= numWorkers; i++ {
        wg.Add(1)
        go worker(i, jobs, results, &wg)
    }
    
    // 发送任务
    for i := 1; i <= numJobs; i++ {
        jobs <- Job{ID: i}
    }
    close(jobs)
    
    // 等待所有 worker 完成
    go func() {
        wg.Wait()
        close(results)
    }()
    
    // 收集结果
    for result := range results {
        fmt.Printf("Job %d result: %d\n", result.Job.ID, result.Sum)
    }
}
```

## 高级模式

### 5. Timeout 模式

超时控制模式。

```go
package main

import (
    "fmt"
    "time"
)

func doWork() <-chan string {
    result := make(chan string)
    go func() {
        defer close(result)
        time.Sleep(2 * time.Second)  // 模拟耗时操作
        result <- "work done"
    }()
    return result
}

func main() {
    result := doWork()
    
    select {
    case res := <-result:
        fmt.Println(res)
    case <-time.After(1 * time.Second):
        fmt.Println("timeout!")
    }
}
```

### 6. Context 模式

使用 Context 进行取消和超时控制。

```go
package main

import (
    "context"
    "fmt"
    "time"
)

func doWorkWithContext(ctx context.Context) <-chan string {
    result := make(chan string)
    go func() {
        defer close(result)
        
        for i := 0; i < 5; i++ {
            select {
            case <-ctx.Done():
                fmt.Println("work cancelled")
                return
            case <-time.After(500 * time.Millisecond):
                fmt.Printf("working... %d\n", i)
            }
        }
        
        result <- "work done"
    }()
    return result
}

func main() {
    // 创建带超时的 context
    ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
    defer cancel()
    
    result := doWorkWithContext(ctx)
    
    select {
    case res := <-result:
        fmt.Println(res)
    case <-ctx.Done():
        fmt.Println("timeout or cancelled:", ctx.Err())
    }
}
```

### 7. Semaphore 模式

信号量模式用于限制并发数量。

```go
package main

import (
    "fmt"
    "sync"
    "time"
)

type Semaphore chan struct{}

func NewSemaphore(maxConcurrency int) Semaphore {
    return make(Semaphore, maxConcurrency)
}

func (s Semaphore) Acquire() {
    s <- struct{}{}
}

func (s Semaphore) Release() {
    <-s
}

func main() {
    sem := NewSemaphore(3)  // 最多 3 个并发
    var wg sync.WaitGroup
    
    for i := 1; i <= 10; i++ {
        wg.Add(1)
        go func(id int) {
            defer wg.Done()
            
            sem.Acquire()
            defer sem.Release()
            
            fmt.Printf("Task %d started\n", id)
            time.Sleep(time.Second)
            fmt.Printf("Task %d finished\n", id)
        }(i)
    }
    
    wg.Wait()
}
```

### 8. Rate Limiting 模式

速率限制模式。

```go
package main

import (
    "fmt"
    "time"
)

func main() {
    // 每秒最多处理 2 个请求
    limiter := time.Tick(500 * time.Millisecond)
    
    requests := make(chan int, 10)
    for i := 1; i <= 10; i++ {
        requests <- i
    }
    close(requests)
    
    for req := range requests {
        <-limiter  // 等待限流器
        fmt.Printf("Request %d processed at %v\n", req, time.Now())
    }
}
```

### 9. Circuit Breaker 模式

熔断器模式。

```go
package main

import (
    "errors"
    "fmt"
    "sync"
    "time"
)

type State int

const (
    StateClosed State = iota
    StateOpen
    StateHalfOpen
)

type CircuitBreaker struct {
    maxFailures  int
    timeout      time.Duration
    failures     int
    lastFailTime time.Time
    state        State
    mu           sync.RWMutex
}

func NewCircuitBreaker(maxFailures int, timeout time.Duration) *CircuitBreaker {
    return &CircuitBreaker{
        maxFailures: maxFailures,
        timeout:     timeout,
        state:       StateClosed,
    }
}

func (cb *CircuitBreaker) Call(fn func() error) error {
    cb.mu.Lock()
    defer cb.mu.Unlock()
    
    // 检查是否可以尝试恢复
    if cb.state == StateOpen {
        if time.Since(cb.lastFailTime) > cb.timeout {
            cb.state = StateHalfOpen
            cb.failures = 0
        } else {
            return errors.New("circuit breaker is open")
        }
    }
    
    // 执行函数
    err := fn()
    
    if err != nil {
        cb.failures++
        cb.lastFailTime = time.Now()
        
        if cb.failures >= cb.maxFailures {
            cb.state = StateOpen
        }
        return err
    }
    
    // 成功，重置状态
    if cb.state == StateHalfOpen {
        cb.state = StateClosed
    }
    cb.failures = 0
    
    return nil
}

func main() {
    cb := NewCircuitBreaker(3, 5*time.Second)
    
    // 模拟调用
    for i := 0; i < 10; i++ {
        err := cb.Call(func() error {
            // 模拟失败
            if i < 5 {
                return errors.New("service error")
            }
            return nil
        })
        
        if err != nil {
            fmt.Printf("Call %d failed: %v\n", i, err)
        } else {
            fmt.Printf("Call %d succeeded\n", i)
        }
        
        time.Sleep(time.Second)
    }
}
```

### 10. Pub/Sub 模式

发布订阅模式。

```go
package main

import (
    "fmt"
    "sync"
    "time"
)

type PubSub struct {
    mu          sync.RWMutex
    subscribers map[string][]chan interface{}
}

func NewPubSub() *PubSub {
    return &PubSub{
        subscribers: make(map[string][]chan interface{}),
    }
}

func (ps *PubSub) Subscribe(topic string) <-chan interface{} {
    ps.mu.Lock()
    defer ps.mu.Unlock()
    
    ch := make(chan interface{}, 10)
    ps.subscribers[topic] = append(ps.subscribers[topic], ch)
    return ch
}

func (ps *PubSub) Publish(topic string, msg interface{}) {
    ps.mu.RLock()
    defer ps.mu.RUnlock()
    
    for _, ch := range ps.subscribers[topic] {
        go func(c chan interface{}) {
            c <- msg
        }(ch)
    }
}

func main() {
    ps := NewPubSub()
    
    // 订阅者 1
    sub1 := ps.Subscribe("news")
    go func() {
        for msg := range sub1 {
            fmt.Printf("Subscriber 1 received: %v\n", msg)
        }
    }()
    
    // 订阅者 2
    sub2 := ps.Subscribe("news")
    go func() {
        for msg := range sub2 {
            fmt.Printf("Subscriber 2 received: %v\n", msg)
        }
    }()
    
    // 发布消息
    time.Sleep(time.Second)
    ps.Publish("news", "Breaking news!")
    ps.Publish("news", "Another update!")
    
    time.Sleep(2 * time.Second)
}
```

## 实战案例

### 案例 1：并发爬虫

```go
package main

import (
    "fmt"
    "sync"
    "time"
)

type URL string

type Fetcher interface {
    Fetch(url URL) (urls []URL, err error)
}

type SafeCache struct {
    mu    sync.Mutex
    cache map[URL]bool
}

func (c *SafeCache) Set(url URL) bool {
    c.mu.Lock()
    defer c.mu.Unlock()
    
    if c.cache[url] {
        return false
    }
    c.cache[url] = true
    return true
}

func Crawl(url URL, depth int, fetcher Fetcher, cache *SafeCache, wg *sync.WaitGroup) {
    defer wg.Done()
    
    if depth <= 0 {
        return
    }
    
    if !cache.Set(url) {
        return
    }
    
    urls, err := fetcher.Fetch(url)
    if err != nil {
        fmt.Println(err)
        return
    }
    
    fmt.Printf("Found: %s\n", url)
    
    for _, u := range urls {
        wg.Add(1)
        go Crawl(u, depth-1, fetcher, cache, wg)
    }
}

// 模拟 Fetcher
type fakeFetcher map[URL][]URL

func (f fakeFetcher) Fetch(url URL) ([]URL, error) {
    if urls, ok := f[url]; ok {
        time.Sleep(100 * time.Millisecond)
        return urls, nil
    }
    return nil, fmt.Errorf("not found: %s", url)
}

func main() {
    fetcher := fakeFetcher{
        "https://golang.org/": {
            "https://golang.org/pkg/",
            "https://golang.org/cmd/",
        },
        "https://golang.org/pkg/": {
            "https://golang.org/pkg/fmt/",
            "https://golang.org/pkg/os/",
        },
    }
    
    cache := &SafeCache{cache: make(map[URL]bool)}
    var wg sync.WaitGroup
    
    wg.Add(1)
    Crawl("https://golang.org/", 3, fetcher, cache, &wg)
    wg.Wait()
}
```

### 案例 2：并发下载器

```go
package main

import (
    "fmt"
    "sync"
    "time"
)

type Download struct {
    URL      string
    Progress int
}

func downloadFile(url string, results chan<- Download, wg *sync.WaitGroup) {
    defer wg.Done()
    
    for i := 0; i <= 100; i += 10 {
        time.Sleep(100 * time.Millisecond)
        results <- Download{
            URL:      url,
            Progress: i,
        }
    }
}

func main() {
    urls := []string{
        "https://example.com/file1.zip",
        "https://example.com/file2.zip",
        "https://example.com/file3.zip",
    }
    
    results := make(chan Download, len(urls)*10)
    var wg sync.WaitGroup
    
    // 启动下载
    for _, url := range urls {
        wg.Add(1)
        go downloadFile(url, results, &wg)
    }
    
    // 等待完成并关闭 channel
    go func() {
        wg.Wait()
        close(results)
    }()
    
    // 显示进度
    for download := range results {
        fmt.Printf("%s: %d%%\n", download.URL, download.Progress)
    }
}
```

## 最佳实践

### 1. 选择合适的模式

- 简单任务：Generator
- 数据处理：Pipeline
- 高并发：Worker Pool
- 限流：Rate Limiting
- 容错：Circuit Breaker

### 2. 避免常见错误

```go
// ❌ 忘记关闭 channel
func bad() <-chan int {
    ch := make(chan int)
    go func() {
        ch <- 1
        // 忘记 close(ch)
    }()
    return ch
}

// ✅ 正确关闭
func good() <-chan int {
    ch := make(chan int)
    go func() {
        defer close(ch)
        ch <- 1
    }()
    return ch
}
```

### 3. 使用 Context 控制生命周期

```go
func worker(ctx context.Context) {
    for {
        select {
        case <-ctx.Done():
            return
        default:
            // 工作
        }
    }
}
```

## 学习检查清单

- [ ] 理解 CSP 并发模型
- [ ] 掌握基础并发模式
- [ ] 掌握高级并发模式
- [ ] 能够选择合适的模式
- [ ] 理解各模式的适用场景
- [ ] 能够组合使用多种模式
- [ ] 掌握并发调试技巧

## 相关资源

- [Go Concurrency Patterns](https://go.dev/blog/pipelines)
- [Advanced Go Concurrency Patterns](https://go.dev/blog/io2013-talk-concurrency)
- [Concurrency in Go](https://www.oreilly.com/library/view/concurrency-in-go/9781491941294/)

---

@author erik.zhou
